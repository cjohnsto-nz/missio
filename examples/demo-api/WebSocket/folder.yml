info:
  name: WebSocket
  type: folder
  description: Run node examples/demo-api/server.js before sending these local WebSocket demo requests.
request:
  headers:
    - name: X-Demo-Client
      value: missio-demo
