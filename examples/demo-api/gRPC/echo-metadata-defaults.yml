info:
  name: Echo metadata defaults
  type: grpc
grpc:
  url: "{{grpcBaseUrl}}"
  method: missio.demo.DemoService/EchoUnary
  methodType: unary
  protoFilePath: proto/services/missio_demo.proto
  metadata:
    - name: x-demo-request
      value: metadata-defaults-{{grpcRequestId}}
  message: |-
    {
      "name": "Metadata {{grpcName}}",
      "userId": {{grpcUserId}},
      "trace": {
        "requestId": "{{grpcRequestId}}"
      }
    }
docs: Run `node examples/demo-api/grpc-server.js` from the repository root, select the LOCAL environment, then send this unary gRPC request to verify collection, folder, and request metadata merging at `localhost:50051`.
