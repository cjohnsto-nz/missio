info:
  name: Get User by ID
  type: http
  seq: 2
  tags:
    - users
http:
  method: GET
  url: "{{baseUrl}}/users/{{userId}}"
  headers: []
  params: []
settings:
  timeout: 30000
  encodeUrl: true
  followRedirects: true
  maxRedirects: 5
