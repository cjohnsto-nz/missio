info:
  name: Runtime
  type: folder
  description: Run `node examples/demo-api/server.js` before sending these runtime demo requests.
request:
  headers:
    - name: X-Demo-Folder
      value: runtime

