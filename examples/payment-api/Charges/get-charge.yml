info:
  name: Get Charge
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/{{apiVersion}}/charges/{{chargeId}}"
  headers: []
  params: []

runtime:
  variables:
    - name: chargeId
      value: "ch_test_123"

settings:
  timeout: 30000
  encodeUrl: true
  followRedirects: true
  maxRedirects: 5
